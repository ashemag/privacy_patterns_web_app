Install the same packages using the same versions:
Remember to exclude the virtual environment folder from source control by adding it to the ignore list

# to save 
$ pip freeze > requirements.txt

# to fetch 
$ pip install -r requirements.txt

-Django only supports python3 now RIP 
